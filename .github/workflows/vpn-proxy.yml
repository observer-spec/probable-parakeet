name: Tailscale VPN & Proxy

# Rolling handoff: NO concurrency cap on purpose. A new run starts every 5h,
# verifies its own Tailscale node is healthy, then cancels the older run.
# Steady state is 1 runner; 2 overlap only during the ~3 min handoff window.
concurrency:
  group: vpn-proxy-rolling
  cancel-in-progress: false

on:
  schedule:
    # Every 5 hours. Runtime (sleep) is also 5h so the next run starts
    # ~when the current one's time is up -> ~30 min safety overlap
    # is NOT needed; boot+setup (~3 min) is the only overlap.
    - cron: '0 */5 * * *'
  workflow_dispatch:
    inputs:
      duration_hours:
        description: 'Runtime duration in hours (1-6)'
        required: false
        default: '6'
      enable_exit_node:
        description: 'Advertise as Tailscale Exit Node (Full VPN routing)'
        type: boolean
        required: false
        default: true
      enable_socks5:
        description: 'Enable SOCKS5 proxy on port 1080'
        type: boolean
        required: false
        default: true
      enable_http_proxy:
        description: 'Enable HTTP proxy on port 8080'
        type: boolean
        required: false
        default: true

permissions:
  contents: write
  actions: write # needed for the handoff step to cancel the older run

jobs:
  vpn-proxy:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Linux Kernel Networking
        run: |
          echo "net.ipv4.ip_forward = 1" | sudo tee -a /etc/sysctl.d/99-tailscale.conf
          echo "net.ipv6.conf.all.forwarding = 1" | sudo tee -a /etc/sysctl.d/99-tailscale.conf
          sudo sysctl -p /etc/sysctl.d/99-tailscale.conf
          sudo iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
          sudo ip6tables -t nat -A POSTROUTING -o eth0 -j MASQUERADE 2>/dev/null || true

      - name: Tailscale Connect & Proxy Init
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
          ENABLE_EXIT_NODE: ${{ github.event_name == 'workflow_dispatch' && inputs.enable_exit_node || true }}
          ENABLE_SOCKS5: ${{ github.event_name == 'workflow_dispatch' && inputs.enable_socks5 || true }}
          ENABLE_HTTP_PROXY: ${{ github.event_name == 'workflow_dispatch' && inputs.enable_http_proxy || true }}
        run: |
          test -n "$TAILSCALE_AUTHKEY" || { echo "::error::TAILSCALE_AUTHKEY secret is empty"; exit 1; }

          curl -fsSL https://tailscale.com/install.sh | sh

          DAEMON_FLAGS=""
          if [ "$ENABLE_SOCKS5" = "true" ]; then
            DAEMON_FLAGS="$DAEMON_FLAGS --socks5-server=0.0.0.0:1080"
          fi
          if [ "$ENABLE_HTTP_PROXY" = "true" ]; then
            DAEMON_FLAGS="$DAEMON_FLAGS --outbound-http-proxy-listen=0.0.0.0:8080"
          fi

          if [ -n "$DAEMON_FLAGS" ]; then
            echo "FLAGS=\"$DAEMON_FLAGS\"" | sudo tee -a /etc/default/tailscaled
            sudo systemctl restart tailscaled
            sleep 2
          fi

          UP_ARGS="--hostname=gh-vpn-proxy --accept-routes --reset"
          if [ "$ENABLE_EXIT_NODE" = "true" ]; then
            UP_ARGS="$UP_ARGS --advertise-exit-node"
          fi

          echo "Running tailscale up $UP_ARGS"
          sudo tailscale up --authkey="$TAILSCALE_AUTHKEY" $UP_ARGS

          TS_IP=$(tailscale ip -4)
          echo "TAILSCALE_IP=$TS_IP" >> $GITHUB_ENV
          echo "Tailscale Node IP: $TS_IP"

      - name: Publish Live Endpoint Docs
        env:
          TS_IP: ${{ env.TAILSCALE_IP }}
          GH_TOKEN: ${{ github.token }}
        run: |
          printf "# Active Tailscale VPN & Proxy Endpoint\n\n- **Node Name:** \`gh-vpn-proxy\`\n- **Tailscale IPv4:** \`%s\`\n- **Exit Node Mode:** %s\n- **SOCKS5 Proxy:** \`%s:1080\`\n- **HTTP Proxy:** \`%s:8080\`\n- **Started:** %s UTC\n\n### How to use:\n1. **As Full VPN (Exit Node):**\n   - On Tailscale app -> Exit Nodes -> Select \`gh-vpn-proxy\`.\n2. **As SOCKS5 Proxy:**\n   - SOCKS5: \`%s:1080\`\n3. **As HTTP Proxy:**\n   - HTTP: \`%s:8080\`\n" "$TS_IP" "${{ inputs.enable_exit_node }}" "$TS_IP" "$TS_IP" "$(date -u +'%Y-%m-%d %H:%M:%S')" "$TS_IP" "$TS_IP" > CURRENT_VPN_ENDPOINT.md

          git config user.name "observer-spec"
          git config user.email "observer@amnuaysilpa.ac.th"
          git add CURRENT_VPN_ENDPOINT.md
          git diff --staged --quiet || (git commit -m "docs: publish active Tailscale VPN endpoint [skip ci]" && git push origin main)

          cat CURRENT_VPN_ENDPOINT.md >> $GITHUB_STEP_SUMMARY

      - name: Verify New Node & Hand Off (cancel older run)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 1. Health check: our own node must be up, have an IPv4,
          #    report itself Online, and advertise itself as exit node.
          TS_IP=$(tailscale ip -4)
          test -n "$TS_IP" || { echo "::error::no Tailscale IPv4"; exit 1; }
          tailscale status | grep -q "$TS_IP" || { echo "::error::self IP $TS_IP not in tailscale status"; exit 1; }
          tailscale ping --c=1 "$TS_IP" || { echo "::error::self ping failed"; exit 1; }
          tailscale exit-node list | grep -q 'gh-vpn-proxy' || { echo "::error::not advertising as exit node"; exit 1; }
          echo "New node healthy: $TS_IP"

          # 2. Give clients ~60s to discover the new node before killing the old one.
          sleep 60

          # 3. Cancel any OLDER in-progress run of this same workflow.
          #    (Only databaseIds smaller than ours -> never suicide, never kill newer.)
          RUN_ID="${{ github.run_id }}"
          gh run list --workflow vpn-proxy.yml --status in_progress --limit 10 \
            --json databaseId -q '.[].databaseId' | while read -r OLD_ID; do
            if [ "$OLD_ID" -lt "$RUN_ID" ]; then
              echo "Handing off: cancelling older run $OLD_ID"
              gh api "repos/${{ github.repository }}/actions/runs/$OLD_ID/cancel" -X POST
            fi
          done

      - name: Keep Tunnel Alive
        run: |
          HOURS=${{ github.event_name == 'workflow_dispatch' && inputs.duration_hours || '5' }}
          SECONDS=$(awk "BEGIN {print int($HOURS * 3600)}")
          echo "Keeping VPN Proxy alive for $HOURS hours ($SECONDS seconds)..."
          sleep $SECONDS
